import React from 'react'
import ReactDOM from 'react-dom/client'
import SecurePathPSP from './SecurePathPSP'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SecurePathPSP />
  </React.StrictMode>
)